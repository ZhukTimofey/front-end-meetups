import { ToastOptions } from "react-toastify";

export const notifications: ToastOptions = {
  position: "top-right",
  hideProgressBar: true,
  closeOnClick: true,
  draggable: true,
  progress: undefined,
};
